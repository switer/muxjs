{
  "name": "muxjs",
  "version": "0.0.0",
  "description": "Modeling app states with muxjs.",
  "main": "index.js",
  "scripts": {
    "test": "mocha test/index.js",
    "dist": "browserify --standalone Mux ./index.js > dist/mux.js && uglifyjs -c hoist_vars=true dist/mux.js > dist/mux.min.js",
    "cover": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | coveralls.js && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/switer/mux.git"
  },
  "keywords": [
    "state",
    "model"
  ],
  "author": "switer",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/switer/mux/issues"
  },
  "homepage": "https://github.com/switer/mux",
  "devDependencies": {
    "coveralls": "^2.11.2",
    "istanbul": "^0.3.5",
    "mocha": "^2.1.0",
    "mocha-lcov-reporter": "0.0.1"
  }
}
